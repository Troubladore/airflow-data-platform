# OpenMetadata Configuration
# ==========================
# Copy this to .env and customize

# ==========================================
# OpenMetadata UI
# ==========================================
# Port for OpenMetadata web interface
OPENMETADATA_PORT=8585

# Cluster name (appears in UI)
OPENMETADATA_CLUSTER_NAME=local-dev

# ==========================================
# Database Configuration
# ==========================================
# OpenMetadata connects to platform-postgres using these credentials
# The password here must match OPENMETADATA_DB_PASSWORD in platform-bootstrap/.env
# (The platform infrastructure creates the user with that password)

DB_USER=openmetadata_user
DB_USER_PASSWORD=changeme_openmetadata_password

# ==========================================
# Image Sources (For Corporate Environments)
# ==========================================
# Default: Public images from Docker Hub / docker.getcollate.io
# Corporate: Set to your Artifactory mirror paths
#
# Example corporate paths (uncomment and customize):
# IMAGE_POSTGRES=artifactory.company.com/docker-remote/library/postgres:17.5-alpine
# IMAGE_OPENMETADATA_SERVER=artifactory.company.com/docker-remote/openmetadata/server:1.10.1
# IMAGE_OPENSEARCH=artifactory.company.com/docker-remote/opensearchproject/opensearch:2.19.2

# ==========================================
# Notes
# ==========================================
#
# OpenSearch Version: Using 2.19.2 (latest supported by OpenMetadata)
# - OpenMetadata officially uses OpenSearch for PostgreSQL deployments
# - Apache 2.0 licensed (better for corporate use than Elasticsearch)
# - Supports versions up to 2.19 per OpenMetadata docs
#
# 1. Access OpenMetadata:
#    - UI: http://localhost:8585
#    - Default login: admin@open-metadata.org / admin
#
# 2. Ingestion via Astronomer DAGs:
#    - See airflow-data-platform-examples for DAG examples
#    - Everything as code!
#
# 3. For corporate Artifactory users:
#    - Run: docker login artifactory.company.com
#    - Set IMAGE_* variables above
#    - Contact DevOps for repository paths
#
