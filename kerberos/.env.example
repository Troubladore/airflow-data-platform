# Kerberos Sidecar Configuration
# ===============================
# Copy this to .env and customize for your environment

# ==========================================
# Company Domain
# ==========================================
# Your organization's Active Directory domain
COMPANY_DOMAIN=COMPANY.COM

# ==========================================
# Kerberos Ticket Configuration
# ==========================================
# Ticket cache location on host
# Run 'klist' to see your ticket location, then set this to the directory:
#
# Examples:
#   Ticket cache: FILE:/tmp/krb5cc_1000
#   → Set: KERBEROS_TICKET_DIR=/tmp
#
#   Ticket cache: DIR::/home/user/.krb5-cache/dev/tkt
#   → Set: KERBEROS_TICKET_DIR=${HOME}/.krb5-cache

KERBEROS_TICKET_DIR=${HOME}/.krb5-cache

# Ticket mode (how sidecar gets tickets):
# - copy: Copies your existing kinit tickets from host (LOCAL DEVELOPMENT)
# - create: Creates tickets via password/keytab (PRODUCTION/K8S)
#
# For local development, use 'copy' - no password needed!
TICKET_MODE=copy

# Ticket refresh interval (seconds) - only used in copy mode
COPY_INTERVAL=300

# ==========================================
# Kerberos Config File
# ==========================================
# Path to krb5.conf on host (usually auto-detected)
KRB5_CONF_PATH=/etc/krb5.conf

# ==========================================
# Notes
# ==========================================
#
# 1. For local development:
#    - kinit USERNAME@DOMAIN (get ticket on host)
#    - make start (sidecar copies and shares ticket)
#    - No password needed in .env
#
# 2. For production/K8s:
#    - Set TICKET_MODE=create
#    - Store password in .secrets/krb_password.txt
#    - Sidecar creates and renews tickets automatically
#
